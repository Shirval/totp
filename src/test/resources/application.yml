spring:
  liquibase:
    enabled: "true"
    change-log: "classpath:db/changelog/changelog.yaml"
  datasource:
    url: "jdbc:postgresql://localhost:5432/totp"
    username: "user"
    password: "password"